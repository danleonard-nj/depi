# Workspace development environment:  pip install -r requirements-dev.txt
#
# Deliberately no upper bounds. The pins that used to live here went stale, pip
# resolved an incompatible fastapi/starlette pair, and the whole suite failed
# for reasons that had nothing to do with depi. Version ranges belong in the CI
# matrix, where a break is visible as a matrix cell rather than as a mystery.

-e packages/depi-core
-e packages/depi-flask
-e packages/depi-quart
-e packages/depi-fastapi
-e packages/depi-django

pytest>=7.4
pytest-asyncio>=0.23
pytest-benchmark>=4.0
httpx>=0.27              # required by fastapi.testclient
ruff>=0.1

# benchmark comparison target, and chart rendering for tests/plot_benchmarks.py
dependency-injector>=4.41
psutil>=5.9
matplotlib>=3.7
