# Flask adapter

`pip install pydepi-flask` — import as `depi_flask`. Usage guide:
[Flask integration](../integrations/flask.md).

::: depi_flask.FlaskInjector
    options:
      inherited_members: true
