# Quart adapter

`pip install pydepi-quart` — import as `depi_quart`. Usage guide:
[Quart integration](../integrations/quart.md).

::: depi_quart.QuartInjector
    options:
      inherited_members: true
